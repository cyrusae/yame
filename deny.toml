# cargo-deny configuration  https://embarkstudios.github.io/cargo-deny/

# ---------------------------------------------------------------------------
# Security advisories (RustSec database)
# ---------------------------------------------------------------------------
[advisories]
# Deny crates with known security vulnerabilities.
version = 2
ignore = [
    # arboard transitive dependency — EOL upstream, not actionable on our end
    { id = "RUSTSEC-2024-0436", reason = "transitive via arboard; no fix available upstream" },
    # syntect → two-face transitive dependency — advisory is abandonment notice only,
    # not a security vulnerability; bincode 1.3.3 is stable and no upgrade path exists
    { id = "RUSTSEC-2025-0141", reason = "transitive via syntect/two-face; abandonment advisory, not a CVE; no fix available" },
]

# ---------------------------------------------------------------------------
# License policy
# ---------------------------------------------------------------------------
[licenses]
version = 2
# Only allow these SPDX identifiers.  All direct and transitive deps of
# yame are MIT or Apache-2.0; tighten this list if that ever changes.
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "BSL-1.0",       # Boost Software License — OSI/FSF approved, used by arboard transitive deps
    "Unicode-3.0",
    "Zlib",
]

# ---------------------------------------------------------------------------
# Dependency bans
# ---------------------------------------------------------------------------
[bans]
# Warn (not deny) on multiple versions of the same crate — transitive deps
# outside our control sometimes pull in two minor versions.  Revisit once
# the dep graph is stable.
multiple-versions = "warn"
wildcards = "allow"
highlight = "all"

# ---------------------------------------------------------------------------
# Source policy
# ---------------------------------------------------------------------------
[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
