# Friends of `yame`

These tools aren't dependencies--you can use `yame` fine without them--but they work together well and can improve your experience! 

When present, `yame` uses `fd` and `fzf` for fuzzy file finding from the command line, and `lf` and `bat` for file navigation and previews.

To access these functions:

## The tool: `fd` and `fzf`

### Why you want this:

### How to install:


## The tool: `lf`

### Why you want this:

### How to install:


## The tool: `bat`

### Why you want this:

### How to install:
