# Notes for crate

- Exclude `.chainlink` from crate
- Assume future workflow using `release-plz` with a crates.io release (`yame` name is available)
- Set up CI with GitHub as soon as we have tests
- `clippy` and `cargo fmt` frequently
